controller:
  service:
    annotations:
      kubernetes.io/elb.class: union
      kubernetes.io/elb.id: "{{ chart.elb_id }}"
    LoadBalancerIP: "{{ chart.elb_eip }}"
  admissionWebhooks:
    patch:
      image:
        image: quay.io/kucerakk/kube-webhook-certgen

